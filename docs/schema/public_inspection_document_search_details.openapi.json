openapi: 3.1.0
info:
  title: Public Inspection Document Search Details Schema
  version: '1.0.0'
components:
  schemas:
    PublicInspectionDocumentSearchDetails:
      type: object
      description: Details about a public inspection document search, including filters and suggestions.
      properties:
        filters:
          type: array
          items:
            $ref: '#/components/schemas/Filter'
          description: A list of filter objects provided by the API.
        suggestions:
          type: array
          items:
            $ref: '#/components/schemas/Suggestion'
          description: A list of suggestion objects provided by the API.
      required:
        - filters
        - suggestions
