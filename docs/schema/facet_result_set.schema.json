{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "FacetResultSet",
  "description": "A specialized ResultSet for handling responses from facet API endpoints.",
  "type": "object",
  "properties": {
    "conditions": {
      "type": "object",
      "description": "The query conditions used to generate this facet result set.",
      "additionalProperties": true
    },
    "results": {
      "type": "array",
      "description": "An array of Facet items. The actual schema of items depends on the specific facet type.",
      "items": {
        "$ref": "#/components/schemas/Facet" 
      }
    }
  },
  "required": [
    "conditions",
    "results"
  ],
  "additionalProperties": false,
  "components": {
    "schemas": {
      "Facet": {
        "type": "object",
        "description": "Placeholder for Facet schema. All facet models inherit from Facet.",
        "additionalProperties": true
      }
    }
  }
}